export namespace Enums {
  export enum UserRoles {
    Admin = 1,
  }

  export enum DisposalMethod {
    Trash,
    Recycle,
    Compost,
  }

  export enum ContainerMaterial {
    PaperOrPlantFiber,
    Plastic,
    Metal
  }
}
